// STUB (CH-05 skeleton) — real Reset-password confirm (CSR) arrives in CH-11.
import { PageStub } from "@/components/PageStub";

export default function ResetPasswordPage() {
  return <PageStub title="Choose a new password" chunk="CH-11" />;
}
